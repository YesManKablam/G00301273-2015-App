<Page
    x:Class="CoolType.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CoolType"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock x:Name="textBlock" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="10,10,10,0" SelectionChanged="textBlock_SelectionChanged" Width="340" Height="182" VerticalAlignment="Top">
        	<Run Text="Select what file you want to open up with the button on screen. Then just type out the word that you see on screen. "/>
        	<LineBreak/>
        	<Run/>
        	<LineBreak/>
        	<Run Text="Hit the next button to check the word and if it's spelled correctly, you get a point. When the file that you picked is completed, the game will end and you can check your total score."/>
        </TextBlock>
        <Button x:Name="backBtn" Content="Back to Main" HorizontalAlignment="Center" Margin="0,350,0,0" VerticalAlignment="Top" Click="backBtn_Click"/>

    </Grid>
</Page>
